name: docker
on:
  push:
    branches:
      - "main"
jobs:
  test:
    name: Run tests in docker container
    runs-on: ubuntu-latest
    env:
      DOCKER_USER: ${{ secrets.DOCKER_USER }}
      DOCKER_PASS: ${{ secrets.DOCKER_PASS }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Clean up runner
      run: |
        # Remove Java (JDKs)
        sudo rm -rf /usr/lib/jvm
  
        # Remove .NET SDKs
        sudo rm -rf /usr/share/dotnet
  
        # Remove Swift toolchain
        sudo rm -rf /usr/share/swift
  
        # Remove Haskell (GHC)
        sudo rm -rf /usr/local/.ghcup
  
        # Remove Julia
        sudo rm -rf /usr/local/julia*
  
        # Remove Android SDKs
        sudo rm -rf /usr/local/lib/android
  
        # Remove Chromium (optional if not using for browser tests)
        sudo rm -rf /usr/local/share/chromium
  
        # Remove Microsoft/Edge and Google Chrome builds
        sudo rm -rf /opt/microsoft /opt/google
  
        # Remove Azure CLI
        sudo rm -rf /opt/az
  
        # Remove PowerShell
        sudo rm -rf /usr/local/share/powershell
  
        # Remove CodeQL and other toolcaches
        sudo rm -rf /opt/hostedtoolcache
  
        docker system prune -af || true
        docker builder prune -af || true
        df -h

    - name: Create .env file
      run: cp env .env

    - name: Build docker images
      run: docker compose -f docker-compose.build.yml build

    - name: Tag built image for CPU test run
      run: docker tag computervision:0.0.1 ghcr.io/ccb-hms/computervision:latest

    - name: Run tests
      run: docker compose -f docker-compose.cpu.yml run --rm app python -m pytest

  test-ghcr-image:
    name: Run pytest in published GHCR image
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    env:
      MSYS_NO_PATHCONV: 1

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Switch Docker Desktop to Linux containers
      if: runner.os == 'Windows'
      shell: pwsh
      run: '& "$env:ProgramFiles\Docker\Docker\DockerCli.exe" -SwitchDaemon'

    - name: Wait for Linux container engine
      if: runner.os == 'Windows'
      run: |
        for i in $(seq 1 30); do
          if docker info --format '{{.OSType}}' 2>/dev/null | grep -qi linux; then
            echo "Docker is running Linux containers"
            exit 0
          fi
          echo "Waiting for Docker to switch to Linux containers... ($i/30)"
          sleep 5
        done
        echo "Docker did not switch to Linux containers in time" >&2
        exit 1

    - name: Create .env file
      run: cp env .env

    - name: Create data directory
      run: mkdir -p data

    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

    - name: Pull published image
      run: docker pull ghcr.io/ccb-hms/computervision:latest

    - name: Run tests
      run: |
        docker run --rm \
          --ipc=host \
          --env-file .env \
          -e HF_HOME=/app/data \
          -e TORCH_HOME=/app/data \
          -e DATA_DIR=/app/data \
          -v "${{ github.workspace }}:/app" \
          -v "${{ github.workspace }}/data:/app/data" \
          ghcr.io/ccb-hms/computervision:latest \
          python -m pytest